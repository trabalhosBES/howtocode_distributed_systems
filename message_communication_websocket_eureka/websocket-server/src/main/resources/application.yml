# application.yml

server:
  port: 8761 # or your preferred port

spring:
  application:
    name: websocket-server

eureka:
  client:
    healthcheck:
      enabled: true
    register-with-eureka: false
    fetch-registry: false